# ======================================================
# Kotak Neo 5-Minute Volatility Harvester
# Environment Variables — Copy to .env and fill values
# ======================================================

# --- Kotak Neo API (v2 SDK) ---
# Get your consumer key: Neo app/web → Invest tab → Trade API card → Generate Application → copy token
KOTAK_CONSUMER_KEY=your_consumer_token_here

# Registered mobile number WITH country code (e.g., +91XXXXXXXXXX)
KOTAK_MOBILE=+91XXXXXXXXXX

# Unique Client Code — find in Neo app/web → Profile section
KOTAK_UCC=your_ucc_here

# Neo account MPIN (used to validate TOTP login)
KOTAK_MPIN=your_mpin_here

# --- TOTP Secret (Base32 encoded) ---
# Register for TOTP at https://www.kotaksecurities.com/platform/kotak-neo-trade-api/
# When shown QR code, click "Can't scan?" to get the Base32 secret
TOTP_SECRET=your_totp_secret_base32

# --- Google Sheets ---
# Path to service account JSON credentials file
GOOGLE_CREDS_PATH=./config/google_credentials.json
# Spreadsheet ID (from URL: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit)
SPREADSHEET_ID=your_spreadsheet_id_here

# --- System Configuration ---
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# Path to store log files
LOG_DIR=./logs
# Path to checkpoint directory
CHECKPOINT_DIR=./data/checkpoints
# Path to fallback storage
FALLBACK_DIR=./data/fallback
